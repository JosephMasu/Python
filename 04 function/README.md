# 04 Function — Inputs & Defaults

Practice with `input()` and functions that use default arguments.

## Files
- `input.py` — collect user input
- `defaultargs.py` — functions with default parameters

## Run
```bash
python input.py
python defaultargs.py
```
