# Madlibs Generator

Fill placeholders in a story template (`story.txt`) with your own words.

## Files
- `project.py` — Mad Libs app
- Uses `story.txt` (keep it in the same folder or project root as configured)

## Run
```bash
cd "Malibs Generator"
python project.py
```
