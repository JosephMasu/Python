# Currency Converter

CLI currency converter using the free Frankfurter API (no API key required).

## Requirements
```bash
pip install requests
```

## Commands
- `list` — show currencies
- `convert` — convert an amount
- `rate` — show exchange rate
- `q` — quit

## Run
```bash
python project.py
```
