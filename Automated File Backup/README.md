# Automated File Backup

Copies a source folder (Screenshots) into a dated backup folder on a schedule.

## Requirements
```bash
pip install schedule
```

## Configure
Edit these paths in `project.py` if needed:
- Source: `~/Pictures/Screenshots`
- Destination: `~/Desktop/Backups`

## Run
```bash
python project.py
```
